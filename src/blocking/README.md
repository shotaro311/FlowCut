# blocking モジュール

ブロック分割ロジックを管理します。音声認識結果（Sentence列）を最大1200文字/30秒単位で分割し、末尾1-2文を次ブロックへ重複コピーします。LLM整形前の下ごしらえを担い、将来的な再実行や`--resume` を考慮した再現性を確保します。

- `splitter.py`: `BlockSplitter`, `Sentence`, `Block` 等のデータモデルとロジック本体
- 今後: ブロック評価や補助的なトリミングを実装予定
