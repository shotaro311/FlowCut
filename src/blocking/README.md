# blocking モジュール

ブロック分割ロジックを管理します。基本方針は「句読点など意味の切れ目を優先し、過剰な強制カットはしない」。最大2000文字を安全弁とし、末尾1-2文を次ブロックへ重複コピーします。LLM整形前の入力を大きめに保ちつつ、異常に長い無句読点文のみを分割して安定性を担保します。`--resume` を考慮した再現性を確保します。

- `splitter.py`: `BlockSplitter`, `Sentence`, `Block` 等のデータモデルとロジック本体
- 今後: ブロック評価や補助的なトリミングを実装予定
